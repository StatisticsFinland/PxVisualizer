/**
 * Configurations for charts that are not defined in input object (IQueryVisualizationResponse)
 */
export interface IChartOptions {
    accessibilityMode?: boolean;
    showTitles?: boolean;
    fontFamily?: string;
    showLastUpdated?: boolean;
    showUnits: boolean;
    showSources: boolean;
}